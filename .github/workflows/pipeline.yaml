name: Orquestrador Principal

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Ambiente de Deploy'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  ledger-pipeline:
    uses: ./.github/workflows/ledger-pipeline.yaml
    with:
      ambiente: ${{ github.event.inputs.target_env }}