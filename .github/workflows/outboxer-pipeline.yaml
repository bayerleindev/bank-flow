name: Outboxer Tests

on:
  workflow_call:
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true
  workflow_dispatch:

env:
  DOCKER_REGISTRY_NAMESPACE: ${{ vars.DOCKER_REGISTRY_NAMESPACE || 'gbcamargo' }}

jobs:
  test:
    name: bank-flow-outboxer
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bank-flow-outboxer
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Run tests
        run: ./gradlew test

      - name: Compute image tag
        id: image
        shell: bash
        run: echo "tag=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build image
        run: ./gradlew bootBuildImage --imageName=bank-flow-outboxer:${{ steps.image.outputs.tag }}

      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag image
        run: |
          docker tag bank-flow-outboxer:${{ steps.image.outputs.tag }} $DOCKER_REGISTRY_NAMESPACE/bank-flow-outboxer:${{ steps.image.outputs.tag }}
          docker tag bank-flow-outboxer:${{ steps.image.outputs.tag }} $DOCKER_REGISTRY_NAMESPACE/bank-flow-outboxer:latest

      - name: Push image
        run: |
          docker push $DOCKER_REGISTRY_NAMESPACE/bank-flow-outboxer:${{ steps.image.outputs.tag }}
          docker push $DOCKER_REGISTRY_NAMESPACE/bank-flow-outboxer:latest
